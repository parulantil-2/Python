# Creating a tuple using()
t = (1,2,4,5)

t1 = ()  #empty tuple
t1= (1)  # wrong way to declare tupple with single element
t1= (1,)  #tupple with single element

print(t1)

#Printing the  elements of tuple 
print(t[0])

# Cannot update the values of a tuple
t[0] = 34  # throws an error